```
cmd/kube-scheduler/scheduler.go
main(): 程序入口
|
└─  cmd/kube-scheduler/app/scheduler.go
    NewSchedulerCommand(): 解析命令行选项.
    |
    ├─  cmd/kube-scheduler/app/options/options.go
    |   NewOptions()
    |   |
    |   ├─  newDefaultComponentConfig(): 设置默认的配置项(调度算法).
    |   |   |
    |   |   ├─  pkg/scheduler/apis/config/v1alpha1/defaults.go
    |   |       SetDefaults_KubeSchedulerConfiguration(): 设置默认的调度算法
    |   |
    |
    |
    |
    └─  cmd/kube-scheduler/app/server.go
        Run(): 使用传入的选项对象开始运行, 不会退出.
        |
        └─  runCommand()
            |
            |
            ├─  cmd/kube-scheduler/app/options/options.go
            |   Options.Config(): 初始化 kube client, 创建 pod informer, recorder 之类的
            |   |                   从这点来看, 其实之后创建的 Scheduler{} 对象就很像一个常规的 controller 了.
            |   |
            |   ├─  createClients(): 创建 kube client
            |   |
            |   └─  makeLeaderElectionConfig(), NewSharedInformerFactory(), NewPodInformer()
            |
            └─  Run(): 真正初始化的开始
                |
                ├─  pkg/scheduler/framework/v1alpha1/registry.go
                |   framework.NewRegistry()
                |
                ├─  pkg/scheduler/scheduler.go
                |   New(): Scheduler{} 对象构造函数
                |
                ├─  pkg/scheduler/scheduler.go
                |   Scheduler.Run(): 开启调度循环, 通过 wait.Until() 不停调用 scheduleOne()
                |   |
                |   └─ scheduleOne(): 真正的调度流程(调度), 每次执行都只调度 1 个Pod

```

调度流程, 从`scheduleOne()`开始.

```
pkg/scheduler/scheduler.go
scheduleOne()
|
└─  Scheduler.schedule()
```
